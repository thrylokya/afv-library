query PropertyAddressesByIds($propertyIds: [ID!]) {
	uiapi {
		query {
			Property__c(where: { Id: { in: $propertyIds } }, first: 2000) {
				edges {
					node {
						Id
						Address__c @optional {
							value
							displayValue
						}
					}
				}
			}
		}
	}
}
